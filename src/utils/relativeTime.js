export function formatRelativeTime(value, now=new Date()) { const date=new Date(value); const seconds=Math.max(0,Math.floor((now-date)/1000)); if(seconds<60)return "Agora"; const minutes=Math.floor(seconds/60); if(minutes<60)return `Há ${minutes} ${minutes===1?"minuto":"minutos"}`; const hours=Math.floor(minutes/60); if(hours<24)return `Há ${hours} ${hours===1?"hora":"horas"}`; if(hours<48)return "Ontem"; const days=Math.floor(hours/24); if(days<=7)return `Há ${days} dias`; return date.toLocaleDateString("pt-BR",{timeZone:"America/Sao_Paulo"}); }
